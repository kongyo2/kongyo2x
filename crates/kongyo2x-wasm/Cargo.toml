[package]
name = "kongyo2x-wasm"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "WebAssembly numeric kernels for kongyo2x image super-resolution."
publish = false

[lib]
crate-type = ["cdylib"]
